package no.nav.tag.tiltaksgjennomforing.arena.repository;

import no.nav.tag.tiltaksgjennomforing.arena.models.ords.ArenaOrdsArbeidsgiver;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArenaOrdsArbeidsgiverRepository extends JpaRepository<ArenaOrdsArbeidsgiver, Integer> {
}
