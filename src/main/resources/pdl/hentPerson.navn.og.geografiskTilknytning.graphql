query($ident: ID!) {
  hentPerson(ident: $ident) {
	  navn {
		  fornavn
		  mellomnavn
		  etternavn
	  }
  }
  hentGeografiskTilknytning(ident: $ident){
      gtType
      gtKommune
      gtBydel
      gtLand
      regel
    }
}